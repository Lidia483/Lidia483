class UART:
    def __init__(self, baudrate):
        self.baudrate = baudrate
        print(f"UART initialized with baudrate {self.baudrate}")

    def transmit(self, data):
        print(f"Transmitting: {data}")

    def receive(self):
        print("Receiving data")
        return b""
